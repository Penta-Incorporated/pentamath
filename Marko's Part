/*Takes the persons elo
start at 1000 elo
10 problem mode 50 problem mode and so on
so percentage of problems solved would have 50 subtracted from it 
and anything below a 50 would make you lose elo anything above would make you gain elo
we would have a 50 multiplier on it*/

//Code:
<script>
    var elo = 1200;
    var probAmount = 10;
    var temp;
    var problem;
    var operationArray = new Array();
    operationArray[0] = "+";
    operationArray[1] = "-";
    operationArray[2] = "*";
    operationArray[3] = "/";

    var createProblem = function(elo, input){
        var count = new Array();
        var eloSub = elo;
        for(var q = 0; q<(elo/400); q++){
            
            count[q] = Math.floor(Math.random()*elo);
            if(count[q]%10 === 0){
                eloSub -= 100;
            }
            else{
                eloSub -= 200;
            }
            document.writeln(count[q]);
            document.writeln(eloSub);
        }
        var i = Math.floor(Math.random()*4);
        var j = Math.floor(Math.random()*elo);
        
        
    };
createProblem(elo, 0);
</script>
